# ProtoTest.GraphQL

Reserved for the planned ProtoTest GraphQL integration.

The project is intentionally not packable or published yet. Its future scope is a named GraphQL client, request helpers, protocol-aware assertions, diagnostics, and operation/field coverage built on the shared ProtoTest lifecycle.

Use `ProtoTest.Rest` for HTTP-level GraphQL calls until this integration is implemented.
