# ProtoTest.GraphQL.Demo

A small executable introduction to the ProtoTest GraphQL API. It demonstrates fluent
selection building, shared JSON shape matching, GraphQL errors, an in-process Hot Chocolate
server, and schema coverage.

```powershell
dotnet test samples/ProtoTest.GraphQL.Demo
```
